## 3.1 System integration and technology stack
This study employs a Rhino 9 Grasshopper–Python workflow to parameterize office floor plans and feed the geometry into JuPedSim, a pedestrian dynamics and evacuation simulator. Office layouts are represented in Grasshopper as polylines (to encode polygonal footprints) and point sets (to specify initial agent positions). A Python script imports JuPedSim via its API, converting the Rhino-generated geometry into JuPedSim scene inputs and mapping the Grasshopper points to agent initial positions. JuPedSim’s Social Force Model is used as the core micro-scale model, with the following default settings: desired speed set to 0.8 m/s, and obstacles_scale set to 1000 to balance obstacle forces in cluttered scenes. Each simulation run yields evacuation metrics such as total evacuation time, path formation, and congestion distribution.
- 目标与框架
    - 说明本研究将 Rhino 9 的 Grasshopper 与 JuPedSim 进行参数化耦合，用 Grasshopper 表示办公室平面几何（多边线 Polyline 与点集）、初始代理位置；通过 Python 脚本调用 JuPedSim 的 API，将 Rhino 输出映射到 JuPedSim 场景输入，并在 JuPedSim 内部使用社会力模型完成微观仿真。
- 技术栈与版本
    - Rhino 9（Grasshopper） + Python（3.10+，符合 JuPedSim 的兼容性要求）；
    - JuPedSim（Julich Pedestrian Simulator）及其 Social Force Model；
    - 数据分析与处理：JupyterLab + Python（Pandas、NumPy、Matplotlib/Seaborn、SQLAlchemy/sqlite3）；
    - 数据存储与回放：每次仿真输出一个 SQLite 数据库文件，便于离线回放与统计分析。
- 输入输出描述
    - 输入：办公室几何（Polyline 表示轮廓、门、走道等）、初始代理位置（Grasshopper 点集合）、场景参数（岛式桌布局、走廊宽度、出口数量、门宽等）。
    - 输出：每轮仿真生成的 SQLite 文件，包含轨迹、速度、拥堵事件等诊断信息，以及用于后续分析的元数据（版本、环境、随机种子等）。
- 数据管线
    - 数据从 Rhino/Grasshopper 流入 JuPedSim，仿真结果通过 SQLite 保存；分析阶段在 JupyterLab 中读取 SQLite 数据，执行清洗、统计与可视化。

- Objectives and Framework 
	- This research aims to achieve parametric coupling of Rhino 9's Grasshopper with JuPedSim, using Grasshopper to represent office floor geometry (polylines and point sets) and initial agent positions. A Python script will call JuPedSim's API to map the output from Rhino to the input for the JuPedSim scene, utilizing a social force model for microscopic simulations within JuPedSim. 
	
- Technology Stack and Versions 
	- Rhino 9 (Grasshopper) + Python (3.10+, meeting JuPedSim's compatibility requirements);
	- JuPedSim (Julich Pedestrian Simulator) and its Social Force Model; 
	- Data analysis and processing: JupyterLab + Python (Pandas, NumPy, Matplotlib/Seaborn, SQLAlchemy/sqlite3); 
	- Data storage and playback: Each simulation outputs an SQLite database file for offline playback and statistical analysis. 
	
- Input and Output Description 
	- Input: Office geometry (polylines representing contours, doors, corridors, etc.), initial agent positions (Grasshopper point sets), and scene parameters (island table layout, corridor width, number of exits, door widths, etc.). 
	- Output: SQLite files generated by each simulation round, containing trajectories, speeds, congestion events, and other diagnostic information, along with metadata for subsequent analysis (version, environment, random seed, etc.). 
	
- Data Pipeline - Data flows from Rhino/Grasshopper into JuPedSim, and simulation results are saved through SQLite; during the analysis phase, SQLite data is read in JupyterLab for cleaning, statistics, and visualization.

## 3.2 Experimental design

- 原型尺度与研究目标对应
    - Small 原型：聚焦布局对疏散路径与时间的初步影响，通过较小几何尺寸快速迭代。
    - Big 原型：引入门宽耦合与多房间嵌套等更高层次问题，检验在更接近实际办公空间规模下的耦合效应。
- 随机性与重复
    - 每一参数配置固定进行 5 次重复，初始位置由 Grasshopper 点集合随机抽样得到，以降低随机性对结果的影响。
- 场景参数矩阵（变量设计）
    - 岛式桌布局：A、B、C 三种变体，作为内部家具配置的代表性参数。
    - 走廊宽度：0.8 m, 1.2 m, 1.6 m, 2.0 m, 2.4 m（从小到大覆盖关键区间，便于识别边际效应与可能的阈值）。
    - 出口配置：单出口与双出口，比较不同出口数量对路径形成与拥堵的影响。
    - Big 原型额外变量：门宽（Gate Width）在 1.0 m–1.6 m，探讨早期拥堵与流量分布的敏感性；多房间耦合场景中房间门宽与走廊门宽的组合。
- 操作流程要点
    - 在 Rhino Grasshopper 中构建并以参数化方式导出岛式桌布局与走廊几何；
    - Python 脚本将 Grasshopper 的输出转换为 JuPedSim 的场景输入，设定初始代理位置；
    - 运行 JuPedSim，保存 SQLite 轨迹数据；重复不同配置共计 5 次。

## 3.3 Agents and Model Settings（代理与模型设定）

- 概要
    - 本节聚焦于在 JuPedSim 框架下对代理（pedestrian）属性、初始分布、以及所采用的社会力模型（Social Force Model, SFM）的关键参数进行明确设定与记录，确保不同配置之间具有可比性与可重复性。
- 3.3.1 代理属性与初始条件
    - 初始代理数量与分布：根据场景规模在 Grasshopper/JuPedSim 场景输入中设定代理总数，通过初始分布（ Grasshopper 点集合）控制初始数量与空间分布。
    - 初始位置映射：Grasshopper 的点集合映射为代理的初始坐标，确保不同配置之间的初始位置随机性可控，且重复实验可复现。
    - 目标与行为倾向：默认统一设定，但面对多入口场景，agent会倾向于选择最短路径。
- 3.3.2 社会力模型参数（Social Force Model）
    
    - 模型框架：采用 JuPedSim 内置的 Social Force Model 作为核心微观仿真模型。
    - 目标速度（desired speed）：0.8 m/s，作为默认值，并在需要时进行范围内的敏感性分析（如 0.6–1.0 m/s）。
    - 相互作用力参数：包括代理之间的排斥力（repulsive force）、对障碍物的排斥力、以及对队列化与群体聚集的响应强度等。初始设定采用 JuPedSim 官方推荐值。
    - 障碍物处理与边界条件：障碍物力的尺度（obstacles_scale）设为 1000，用以在复杂场景中平衡对障碍物的抵抗，防止数值过度抑制代理运动。边界条件遵循场景几何输入中的墙体与出口定义。

## 3.4 Data collection and Analysis

- 数据输出与保存
    - 每次仿真产出一个独立的 SQLite 数据库文件，记录代理轨迹、速度、位置、及场景元数据，便于回放与离线分析。
- 数据读取与处理
    - 使用 JupyterLab + Python 读取 SQLite 文件（ sqlite3），将数据整理为可分析的结构（DataFrame），对时间步、代理属性、拥堵指标等进行聚合与统计。
- 指标体系
    - 主要指标：总撤离时间（TET）、平均路径长度、路径分布特征、拥堵热点分布（热力图/密度图）。
    - 次要指标：路径形成模式（单列流、拱形、瓶颈队列化）、重复间方差、路径多样性等。
- 统计分析
    - 对不同布局、走廊宽度、出口配置以及门宽等因素，采用 ANOVA、t 检验或非参数方法进行比较；分析变量之间的交互作用（交互项的显著性）与潜在非线性阈值。
    - 使用回归分析/广义线性模型评估门宽、走廊宽、出口数量等变量的耦合效应对撤离时间与拥堵分布的影响强度。
- 结果呈现与可视化
    - 绘制路径密度图、撤离时间分布直方图、拥堵热点热力图，以及不同配置下的对比柱状图。结合实例轨迹回放，展示具体场景中的路径演化与拥堵点。